spring:
  datasource:
    username: root
    url: jdbc:mysql://localhost:3306/web_store_test?allowPublicKeyRetrieval=true&useSSL=false
    password: Bougassaa2014!
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update

  mail:
    host: smtp-mail.outlook.com
    port: 587
    username: smartstudentshop@outlook.com
    password: ssshop2024
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

  thymeleaf:
    cache: false

server:
  port: 8084
